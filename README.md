# Gold Coast Data Mine live feed

This repository publishes de-identified, aggregate operational metrics for the Gold Coast Data Mine dashboard.

It contains no patient identifiers or clinical information. `status.json` is refreshed every 10 minutes by the local archive companion.
